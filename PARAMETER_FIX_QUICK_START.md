# 快速修复指南：AI 编译参数问题

## 问题已解决 ✅

您遇到的两个问题已经完全修复：

### 问题1️⃣："由于任务未定义参数，使用默认设置"
**之前**：参数区不显示任何内容  
**现在**：✅ 显示task定义的所有参数及其默认值

### 问题2️⃣："运行失败：Extra inputs are not permitted"
**之前**：AI生成的额外参数导致验证失败  
**现在**：✅ 系统自动清理未定义的参数，防止错误

---

## 重启系统使用

修复已集成到代码中，只需重启dashboard：

```bash
# 停止当前运行
Ctrl+C

# 重新启动
python dashboard.py
```

---

## 修复内容速览

### 后端修复（透明执行）

| 组件 | 修复内容 |
|------|---------|
| **编译器** | 返回task的参数定义 + 填入默认值 |
| **验证器** | 自动清理未声明参数 + 警告信息 |
| **API响应** | 增加 `task_parameters` 字段 |

### 前端改进（用户可见）

1. **参数完整显示**
   - 显示task manifest定义的所有参数
   - 每个参数显示其默认值
   - AI生成的值优先覆盖默认值

2. **操作流程** 
   ```
   输入提示 → 点击编译 
   → 查看完整参数列表（包含默认值）
   → 修改任何参数 
   → 点击开始渲染
   ```

3. **安全保障**
   - 无效参数自动过滤
   - 不存在的参数被移除
   - 后端验证更严格

---

## 使用示例

### 场景：生成贷款对比视频

**第1步**：输入提示
```
生成一个500万贷款、利率5%、30年周期的对比视频
```

**第2步**：点击编译
- 系统返回参数面板

**第3步**：查看参数（现在能看到所有参数！）
```
贷款金额（loan_amount）: 2000000  [默认值]
年利率（annual_rate）: 0.042      [默认值]  
贷款年数（loan_years）: 30         [默认值]
输出质量（quality）: preview       [AI生成]
视觉风格（style）: cinematic       [AI生成]
```

**第4步**：修改参数（可选）
- 将贷款金额改为 5000000
- 保留其他参数不变

**第5步**：点击开始渲染
- 所有参数（无论来自默认值还是AI）都会被正确传递
- ✅ 不会出现"Extra inputs"错误

---

## 常见场景

### 场景A：AI生成了不存在的参数
```
✅ 自动清理，显示警告
⚠️  已清理未声明的参数（loan_comparison:loan_animation）: foo_bar
```

### 场景B：API返回所有参数的默认值
```
✅ 前端将这些默认值与AI生成的值合并
✅ 用户看到完整的参数列表
✅ 可编辑所有参数
```

### 场景C：选择没有参数定义的task
```
✅ 正常工作，参数区为空或显示"无参数"
✅ 可正常运行任务
```

---

## 什么改变了

### 用户界面
- ✅ 参数显示更完整
- ✅ 显示默认值
- ✅ 标注参数来源（默认 vs AI生成）

### 后端行为
- ✅ 验证更严格（移除未声明参数）
- ✅ 警告更清晰（显示被清理的参数）
- ✅ 错误更少（Pydantic验证不再失败）

### 兼容性
- ✅ 现有workflow继续工作
- ✅ 现有会话不受影响
- ✅ 无数据库迁移需要

---

## 验证功能是否正常

### 快速测试

**测试1**：参数显示
```bash
1. 打开AI编译面板
2. 输入提示：生成对比视频
3. 点击编译
4. ✅ 应该看到参数列表包含 loan_amount, annual_rate 等默认值
```

**测试2**：参数修改与运行
```bash
1. 编译成功后，修改某个参数值
2. 点击开始渲染
3. ✅ 应该成功运行，不出现"Extra inputs"错误
```

**测试3**：多轮编译
```bash
1. 第一次编译：生成初始参数
2. 在参数区修改值
3. 第二次编译：新提示
4. ✅ 参数列表重新生成，旧修改丢失（预期行为）
```

---

## 还有问题？

如果修复后仍然遇到问题：

1. **清空浏览器缓存**
   - Ctrl+Shift+Delete → 清空所有缓存

2. **重启dashboard**
   ```bash
   Ctrl+C
   python dashboard.py
   ```

3. **检查日志**
   - 终端中查看"⚠️清理"或"❌错误"相关日志

4. **疑难排解**
   - 查看 `AI_PARAMETERS_FIX.md` 了解详细技术细节
   - 查看 `TEST_SESSION_MANAGEMENT.md` 其他相关测试

---

## 技术细节（可选阅读）

完整的修复文档见 [AI_PARAMETERS_FIX.md](AI_PARAMETERS_FIX.md)

修改的代码文件：
- `shared/agent/schemas.py` - 新增 task_parameters 字段
- `shared/agent/compiler.py` - 编译时填充参数定义
- `shared/agent/validate.py` - 验证时清理未声明参数  
- `static/js/dashboard-app.js` - 前端显示所有参数

---

## 总结

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 参数不显示 | 显示task定义 + 默认值 | ✅ 已修复 |
| Extra inputs 错误 | 自动清理参数 | ✅ 已修复 |
| 用户体验 | 参数列表完整可编辑 | ✅ 改善 |

**现在可以开始使用了！** 🚀
