# ============================================================
# 生产运行时依赖（Production Runtime）
# 部署只需安装本文件：pip install -r requirements.txt
# 开发 / 测试还需：pip install -r requirements-dev.txt
# ============================================================

# 数据处理
numpy>=1.21.0
pandas>=1.3.0

# 可视化（核心）
matplotlib>=3.4.0
plotly>=5.18.0
Pillow>=9.0.0

# Web 框架
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-multipart>=0.0.5

# 数据库（Studio 作业持久化）
sqlalchemy>=2.0.0

# HTTP 客户端
requests>=2.28.0
httpx>=0.25.0
python-dateutil>=2.8.2

# 配置与日志
python-dotenv>=0.19.0
python-json-logger>=2.0.0

# 视频处理（成片必需）
ffmpeg-python>=0.2.0
moviepy>=2.0.0
imageio-ffmpeg>=0.4.8

# AI 接口（可选：不配置则仅影响 AI 编译/剧本功能）
openai>=1.0.0

# ============================================================
# 可选依赖（Optional）
# 安装后可解锁对应功能，不安装不影响主控制台
# ============================================================

# kaleido — Plotly 静态图片导出（PNG/SVG）
# pip install kaleido

# opencv-python — 视频帧提取
# pip install opencv-python>=4.5.0

# manim — Manim 动画引擎后端（POC 功能）
# pip install manim>=0.18.0
